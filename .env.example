# configuration base de données
# development (local)
MONGODB_URI=mongodb://localhost:27017/shopjsv2

# production (cluster mongoDB Atlas à configurer dans Northflank)
# MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/shopjsv2-backend?retryWrites=true&w=majority&appName=YOUR_APP

# configuration du serveur
PORT=4000
NODE_ENV=development

# Note pour le déploiement Northflank :
# 1. Créez ces variables d'environnement dans l'interface Northflank
# 2. MONGODB_URI : Votre vraie URL MongoDB Atlas
# 3. NODE_ENV : production
# 4. PORT : Géré automatiquement par Northflank (ne pas définir)